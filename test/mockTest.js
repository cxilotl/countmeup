var assert = require('assert'),
    should = require('should'),
    sinon = require('sinon');

describe('Mock tests to check test run fine', function() {

    it('it should generate an error', function() {

        assert.equal(true, true);

    });

});